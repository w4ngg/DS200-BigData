"""Optional Spark placeholder.

The default demo uses the lighter Python aggregation job because the target
machine has 4 CPU cores and 8 GB RAM. Add a real Spark batch job here if the
report/demo later requires Spark execution.
"""

